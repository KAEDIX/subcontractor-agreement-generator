AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_TENANT_ID=
REDIRECT_URI=

# DocuSeal e-signature API token (X-Auth-Token). Production token — each
# signed document costs $0.20. Never commit the real value.
DOCUSEAL_API_TOKEN=
